# 混合架构 - 快速开始指南

## 🎯 方案概述

```
桌面应用（用户界面）
      ↓ HTTP API
Docker 容器（账号隔离 + 防封号）
```

**优势**：
- ✅ 防封号（Docker 隔离）
- ✅ 用户友好（桌面界面）
- ✅ 可以打包分发
- ✅ 完美方案！

---

## 📁 项目结构

```
whatsapp-translation/
├── desktop-app/              # 桌面应用（Electron）
│   ├── src/
│   │   ├── main.js
│   │   ├── preload.js
│   │   └── index.html
│   └── package.json
│
├── docker/                   # Docker 后端
│   ├── account/              # 账号容器
│   │   ├── Dockerfile
│   │   ├── account-bot.js
│   │   └── package.json
│   │
│   ├── api-gateway/          # API 网关
│   │   ├── Dockerfile
│   │   ├── gateway.js
│   │   └── package.json
│   │
│   └── docker-compose.yml    # 容器编排
│
└── README.md
```

---

## 🚀 快速开始

### 步骤 1：启动 Docker 后端

```bash
# 进入 docker 目录
cd docker

# 启动所有容器
docker-compose up -d

# 查看日志
docker-compose logs -f

# 验证运行
curl http://localhost:3000/api/accounts
```

### 步骤 2：启动桌面应用

```bash
# 进入桌面应用目录
cd desktop-app

# 安装依赖
npm install

# 启动应用
npm start
```

### 步骤 3：使用

1. 桌面应用自动连接到 `http://localhost:3000`
2. 看到账号列表
3. 点击"显示二维码"
4. 扫码登录
5. 开始使用

---

## 🔧 配置

### 添加新账号

编辑 `docker/docker-compose.yml`：

```yaml
  account4:
    build: ./account
    environment:
      - ACCOUNT_ID=account4
      - PROXY_URL=socks5://proxy4.example.com:1080
      - PORT=3004
    ports:
      - "3004:3004"
    volumes:
      - account4-data:/data
```

然后重启：

```bash
docker-compose up -d
```

### 配置代理

在 `docker-compose.yml` 中修改 `PROXY_URL`：

```yaml
environment:
  - PROXY_URL=socks5://your-proxy:1080
```

---

## 📦 打包分发

### 打包桌面应用

```bash
cd desktop-app

# 打包
npm run build

# 生成的文件
dist/WhatsApp Desktop Translation-1.0.0-x64.exe
```

### 分发给用户

**用户需要**：
1. 安装 Docker Desktop
2. 运行 `docker-compose up -d`
3. 运行桌面应用

**或者**：
- 你在服务器上运行 Docker
- 用户只需要桌面应用
- 配置连接到你的服务器

---

## 🌐 远程部署

### 服务器端

```bash
# 在服务器上
cd docker
docker-compose up -d

# 开放端口 3000
```

### 桌面应用配置

修改 `desktop-app/src/config.js`：

```javascript
const API_GATEWAY_URL = 'http://your-server-ip:3000';
```

---

## 💡 使用场景

### 场景 1：个人使用

```
你的电脑
├── Docker Desktop（本地）
└── 桌面应用（连接 localhost）
```

### 场景 2：团队使用

```
服务器
└── Docker 容器

团队成员 1
└── 桌面应用（连接服务器）

团队成员 2
└── 桌面应用（连接服务器）
```

---

## ✅ 优势

1. **防封号**：
   - 每个账号独立容器
   - 独立设备指纹
   - 独立代理/IP

2. **用户友好**：
   - 桌面应用界面
   - 易于使用

3. **灵活部署**：
   - 本地或远程
   - 单用户或多用户

4. **可打包分发**：
   - 打包成 .exe
   - 分发给用户

---

## 🎉 总结

**这是最佳方案！**

- ✅ 防封号（Docker）
- ✅ 用户友好（桌面应用）
- ✅ 可以打包
- ✅ 灵活部署

**你的想法非常正确！** 👍
